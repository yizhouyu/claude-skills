---
name: digest
description: Summarize recent changes in Yizhou's ai_context memory into a digest page (digests/YYYY-MM.md). Use when the user types /digest or asks for a periodic review / monthly summary of his memory.
---

# Digest — periodic ai_context summary

Canonical folder: `/Users/yzy/Library/CloudStorage/GoogleDrive-yizhouyu66@gmail.com/My Drive/ai_context/` (Desktop `ai_context` is a macOS alias to it).

## Steps

1. Find the latest file in `digests/` to determine the last digest date; if none exists, cover everything in `log.md`.
2. Read `log.md` entries since then, and skim the pages they touched.
3. Write `digests/YYYY-MM.md` covering the period:
   - Key life changes (location, work, relationships, health)
   - Decisions made and items vetoed
   - Open threads / unanswered questions
   - Memory-structure changes
   Scannable bullets, headers, Chinese where natural.
4. Append to `log.md`: `## [YYYY-MM-DD] digest | <period covered>`
5. Give Yizhou the highlights in chat, in Chinese.
